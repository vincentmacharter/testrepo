pipeline {
    agent any 
    stages {
        stage('Build') {
            steps {
                echo 'Assigning DNS...'
		sh '~/testrepo/update-public-ip-dns.sh eureka3 default eureka1sts-svc3' 
            }
        }
    }
}

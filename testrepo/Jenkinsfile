pipeline {
    agent any 
    stages {
        stage('Test') {
            steps {
                echo 'Testing Java App...'
                sh 'cp ~/testrepo/testrepo/update-public-ip-dns.sh .'
                sh './update-public-ip-dns.sh eureka1 eurekatest-vincent eureka1svc'
		sh './update-public-ip-dns.sh eureka2 eurekatest-vincent eureka2svc'
 		sh './update-public-ip-dns.sh eureka3 eurekatest-vincent eureka3svc'
                echo 'Assignment complete'
            }
        }
    }
}
